<Page
    x:Class="Gemini.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gemini"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Storyboard x:Name="SlideInAnimation">
            <DoubleAnimation Storyboard.TargetName="InfoPanelTransform"
                             Storyboard.TargetProperty="Y"
                             From="400" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="InfoOverlay"
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Name="SlideOutAnimation">
            <DoubleAnimation Storyboard.TargetName="InfoPanelTransform"
                             Storyboard.TargetProperty="Y"
                             To="400" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="InfoOverlay"
                             Storyboard.TargetProperty="Opacity"
                             To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}" Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" FontSize="20" Width="45" Height="45" Click="HamburgerButton_Click" Background="Transparent"/>
            <TextBlock Grid.Column="1" x:Name="TitleText" x:Uid="TitleText" Text="Чат с Gemini" FontSize="18" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
            <Button Grid.Column="2" x:Name="NewChatButton" Width="45" Height="45" Margin="0,0,10,0" VerticalAlignment="Center" Click="NewChatButton_Click" Background="Transparent">
                <SymbolIcon Symbol="Add"/>
            </Button>
        </Grid>

        <SplitView Grid.Row="1" x:Name="RootSplitView" DisplayMode="CompactOverlay" CompactPaneLength="48" OpenPaneLength="260" IsPaneOpen="False">
            <SplitView.Pane>
                <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <RadioButton x:Name="ChatMenuButton" IsChecked="True" Checked="MenuButton_Checked" GroupName="Menu" Style="{StaticResource TextBlockButtonStyle}" Width="260" Height="48" HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BD;" FontSize="16" VerticalAlignment="Center"/>
                                <TextBlock x:Name="ChatMenuButtonText" x:Uid="ChatMenuButtonText" Text="Чат" Margin="20,0,0,0" VerticalAlignment="Center" FontSize="14" Visibility="Collapsed"/>
                            </StackPanel>
                        </RadioButton>

                        <TextBlock x:Name="HistoryHeader" x:Uid="HistoryHeader" Text="История диалогов" FontSize="12" Foreground="Gray" Margin="15,10,0,5" FontWeight="SemiBold" Visibility="Collapsed"/>
                    </StackPanel>

                    <ListView Grid.Row="1" x:Name="HistoryListView" SelectionChanged="HistoryListView_SelectionChanged" Background="Transparent" Margin="0,5,0,5" Visibility="Collapsed">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Padding="10,6,10,6" Width="250">
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Time}" FontSize="11" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <RadioButton Grid.Row="2" x:Name="SettingsMenuButton" Checked="MenuButton_Checked" GroupName="Menu" Style="{StaticResource TextBlockButtonStyle}" Width="260" Height="48" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SettingsMenuButtonText" x:Uid="SettingsMenuButtonText" Text="Настройки" Margin="20,0,0,0" VerticalAlignment="Center" FontSize="14" Visibility="Collapsed"/>
                        </StackPanel>
                    </RadioButton>
                </Grid>
            </SplitView.Pane>

            <Grid Padding="20">

                <Grid x:Name="ChatPane" Visibility="Visible">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ScrollViewer Grid.Row="0" 
              x:Name="ChatScroll" 
              VerticalScrollMode="Enabled" 
              VerticalScrollBarVisibility="Auto" 
              Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
              Padding="10">

                        <RichTextBlock x:Name="ChatLog" 
                   TextWrapping="Wrap" 
                   FontSize="16" 
                   IsTextSelectionEnabled="True"
                   Margin="0,0,0,10"/>
                    </ScrollViewer>

                    <Grid Grid.Row="1" Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" x:Name="UserInput" x:Uid="UserInput" PlaceholderText="Спросите что-нибудь..." TextWrapping="Wrap" MaxLength="1000" VerticalAlignment="Center" KeyDown="UserInput_KeyDown"/>
                        <Button Grid.Column="1" x:Name="SendButton" Margin="10,0,0,0" Click="SendButton_Click" VerticalAlignment="Center" Width="45" Height="45" Background="Transparent">
                            <SymbolIcon Symbol="Send"/>
                        </Button>
                    </Grid>
                </Grid>

                <Grid x:Name="SettingsPane" Visibility="Collapsed">
                    <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                        <TextBlock x:Uid="ApiKeyLabel" Text="Google AI Studio API Key" FontSize="14" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="ApiKeyInput" PasswordRevealMode="Peek" PlaceholderText="AIzaSy..." Margin="0,0,0,5"/>
                        <TextBlock x:Uid="ApiKeyHint" Text="Ключ сохраняется в локальном изолированном хранилище приложения." FontSize="12" Foreground="Gray" Margin="0,0,0,15"/>

                        <Button x:Uid="SaveSettingsButton" Content="Сохранить настройки" Click="SaveSettings_Click" Margin="0,0,0,25"/>
                        <TextBlock x:Name="StatusMessage" x:Uid="StatusMessage" Foreground="Green" Visibility="Collapsed" Text="Настройки успешно сохранены!" Margin="0,0,0,20"/>

                        <TextBlock x:Uid="ModelLabel" Text="Модель Gemini" FontSize="14" Margin="0,0,0,5"/>
                        <ComboBox x:Name="ModelComboBox" SelectionChanged="ModelComboBox_SelectionChanged" Margin="0,0,0,20" Width="250">
                            <ComboBoxItem Content="gemini-2.5-flash"/>
                            <ComboBoxItem Content="gemini-2.5-pro"/>
                        </ComboBox>

                        <TextBlock x:Uid="ThemeLabel" Text="Тема приложения" FontSize="14" Margin="0,0,0,5"/>
                        <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged" Margin="0,0,0,20" Width="200">
                            <ComboBoxItem x:Uid="ThemeSystem" Content="Как на устройстве"/>
                            <ComboBoxItem x:Uid="ThemeLight" Content="Светлая"/>
                            <ComboBoxItem x:Uid="ThemeDark" Content="Тёмная"/>
                        </ComboBox>

                        <TextBlock x:Uid="LanguageLabel" Text="Язык приложения" FontSize="14" Margin="0,0,0,5"/>
                        <ComboBox x:Name="LanguageComboBox" SelectionChanged="LanguageComboBox_SelectionChanged" Margin="0,0,0,20" Width="200">
                            <ComboBoxItem Content="English" Tag="en-US"/>
                            <ComboBoxItem Content="Русский" Tag="ru-RU"/>
                        </ComboBox>

                        <TextBlock x:Uid="DataManagementHeader" Text="Управление конфигурацией данных" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <Button x:Uid="ClearHistoryButton" Content="Сбросить историю чатов" Click="ClearHistory_Click" Background="#FFE6E6" Foreground="Red" Margin="0,0,0,5"/>
                        <TextBlock x:Name="HistoryStatusMessage" x:Uid="HistoryStatusMessage" Foreground="Green" Visibility="Collapsed" Text="Вся история диалогов успешно удалена!" Margin="0,0,0,20"/>

                        <Button Content="foxgalaxy23" Click="OpenInfoPanel_Click" HorizontalAlignment="Left"/>
                    </StackPanel>
                </Grid>

            </Grid>
        </SplitView>

        <!-- Выдвигающаяся плашка -->
        <Grid x:Name="InfoOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#66000000" Tapped="CloseInfoPanel_Click">
            <Grid x:Name="InfoPanel" VerticalAlignment="Bottom" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
                  BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="0,1,0,0" Tapped="InfoPanel_Tapped">
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="InfoPanelTransform" Y="400"/>
                </Grid.RenderTransform>

                <StackPanel Padding="20" MaxWidth="600" HorizontalAlignment="Center">
                    <TextBlock x:Uid="InfoTitle" Text="Информация" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                    <TextBlock x:Uid="InfoAuthor" Text="Написано foxgalaxy23 в 2026 году" FontSize="14" Margin="0,0,0,5"/>
                    <TextBlock x:Uid="InfoSelfPromo" Text="Самореклама" FontSize="14" Margin="0,0,0,5" FontWeight="SemiBold"/>

                    <HyperlinkButton Content="Социальная сеть Meetlookers.com" NavigateUri="https://meetlookers.com" Margin="0,0,0,5" Padding="0"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock Text="Telegram: " FontSize="14" VerticalAlignment="Center"/>
                        <HyperlinkButton Content="https://t.me/lisisamp" NavigateUri="https://t.me/lisisamp" Padding="0" Margin="5,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="YouTube: " FontSize="14" VerticalAlignment="Center"/>
                        <HyperlinkButton Content="@foxgalaxy23" NavigateUri="https://www.youtube.com/@foxgalaxy23" Padding="0" Margin="5,0,0,0"/>
                    </StackPanel>

                    <Button x:Uid="CloseButton" Content="Закрыть" Click="CloseInfoPanel_Click" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>